export enum PamlightAdminActionTypes {
    START_APP = 'START_APP',
    STOP_APP = 'STOP_APP'
}

export enum PamlightDBWriteTypes {
    CREATE_DOCUMENT = 1,
    CREATE_DOCUMENTS,
    UPDATE_DOCUMENT,
    UPDATE_DOCUMENTS,
    DELETE_DOCUMENT,
    DELETE_DOCUMENTS
}

export enum RouteTypes {
    READ = 1,
    WRITE
}
